# dnf.list — Fedora / RHEL / CentOS packages installed natively (not via Nix).
# One package per line; blank lines and lines starting with # are skipped.
# setup.sh installs only this list when the host runs dnf.
