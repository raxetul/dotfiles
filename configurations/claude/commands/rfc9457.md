---
description: Pin RFC 9457 (Problem Details) as the error-response standard for a web project by writing a rule into its local ./CLAUDE.md.
allowed-tools: Read, Write, Edit, Bash(ls*), Bash(test*), Bash(cat*)
---

Pin the HTTP **error-response standard** for the **current web project** by
adding a short rule to its local `./CLAUDE.md`. This is the reusable
mechanism for the standing preference "apply RFC 9457 across my web
projects" — run it once per web project.

Procedure:

1. **Locate the project file** at the repo root: `./CLAUDE.md`. If it
   doesn't exist yet, you'll create it.

2. **Idempotency / conflict check.** If an `## API error format` section
   already adopts RFC 9457, report "already set" and stop. If it names a
   *different* error convention, show the existing line and confirm
   before replacing it.

3. **Write the rule** — append the section (or create the file with it):

   ```
   ## API error format

   HTTP **error** responses (non-2xx) in this project use **RFC 9457
   Problem Details**, served as `Content-Type: application/problem+json`
   with members `type`, `title`, `status`, `detail`, `instance`, and
   extension members (e.g. an `errors` array of `{pointer, detail}` for
   field-level validation failures). **Successful** responses keep the
   project's house success envelope — RFC 9457 is for errors only.
   Implement both as shared, typed models; controllers must not emit
   ad-hoc error JSON.
   ```

4. **Report** which file was created/edited, and suggest committing it
   using the project's own commit convention — do not commit automatically.

Note: this is intentionally a per-project command rather than an
always-loaded global rule, so it does not load into non-web (embedded /
automotive) projects where it is irrelevant.
